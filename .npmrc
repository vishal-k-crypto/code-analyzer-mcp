# pnpm virtual store configuration
# Store all packages in a central location outside the project
store-dir=${HOME}/.local/share/pnpm/store

# Use isolated node_modules (no hoisting)
shamefully-hoist=false

# Strict peer dependencies
strict-peer-dependencies=true

# Auto-install peers
auto-install-peers=true
